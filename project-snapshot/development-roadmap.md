# Development Roadmap

> Generated by `npm run snapshot` on 2026-07-20T14:00:11.192Z. This snapshot is derived from repository files and does not include secret values.


## Immediate

1. Align Spotify ingestion so CSV imports write to `Spotify_Episode_Metrics`, or explicitly document why `Spotify_Weekly_Snapshot` remains separate.
2. Hit `/api/data-health` against the real base and resolve missing-table or missing-field errors.
3. Verify `/api/channel-breakdown` returns Spotify from `Spotify_Episode_Metrics` in the running environment after restart/deploy.
4. Update stale docs outside `project-snapshot/` or mark them historical.

## Short Term

1. Choose one integration pattern: connector framework, service-specific classes, or a hybrid with clear boundaries.
2. Add persisted sync history/status to Airtable for all connector runs.
3. Add automated tests for Airtable mapping, Spotify channel aggregation, Mailchimp KPI rows, and dashboard route contracts.
4. Decide whether to keep static frontend or create the intended React app in `apps/dashboard`.

## Medium Term

1. Implement live Castos, YouTube, Website, and Meta integrations.
2. Create raw/source Airtable tables for each platform and document canonical mappings.
3. Add scheduler configuration and production-safe observability for connector failures.
4. Harden schema validation into an explicit deployment checklist.

## Long Term

1. Build a platform-agnostic analytics model that each source service implements.
2. Move dashboard UI to a dedicated frontend workspace if React remains the target.
3. Add historical backfill tooling and reconciliation reports.
4. Add role-based access, audit logging, and data quality scoring if this becomes an operational platform.
