# Technical Debt

> Generated by `npm run snapshot` on 2026-07-20T14:00:11.192Z. This snapshot is derived from repository files and does not include secret values.


## Highest Impact

1. Spotify ingestion target mismatch: dashboard reads `Spotify_Episode_Metrics`, but `SpotifyConnector` and `ingestion/spotify` currently map/write `spotifyWeeklySnapshot`.
2. Frontend architecture mismatch: project goal says React Dashboard, but current UI is static JS served from backend.
3. Existing documents in `docs/` contain stale references to `Channel_Performance` and `Spotify_Weekly_Snapshot` as primary sources.
4. Channel Breakdown has split responsibilities: Spotify is assembled in `CommunicationsAnalyticsService`; non-Spotify channels are in `DashboardAggregationService`.

## Medium Impact

- Connector framework has mock connectors plus separate live Mailchimp service, which creates two integration patterns.
- Data source status is in-memory for connector runs unless persisted through Airtable elsewhere.
- Airtable schema validation logs mismatches but does not produce a startup health artifact.
- Several dashboard APIs use tolerant field matching, which helps during migration but can hide schema drift.

## Lower Impact

- `apps/dashboard` is empty and may confuse developers.
- `dist/` is checked into the backend app tree; generated build files can distract from source analysis.
- Legacy KPI sync routes and tables remain available alongside Communications Intelligence APIs.
